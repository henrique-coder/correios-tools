{
  "scripts": {
    "lint": "prettier --check 'extension.config.toml' 'src/**'",
    "format": "prettier --write 'extension.config.toml' 'src/**'"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-toml": "^2.0.6"
  }
}
