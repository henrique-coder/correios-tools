{
  "scripts": {
    "lint": "prettier --check 'extension.config.jsonc' 'src/**'",
    "format": "prettier --write 'extension.config.jsonc' 'src/**'"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
